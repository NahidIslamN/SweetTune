step 1:

copy all file and folder which are nessary for the template


step 2:

replace all keyword "DjangoApp" to "Your project name" form settings.py and celery.py

Goto the chats app and views there replase DjangoApp to "your Project Name"

and replace "Chat App" keyword to "Your project name" form views.py
 

step 3:

create   .env file


step 4:

migrations and migrate





python manage.py runserver

# this one for
celery -A SweetTune worker -B --loglevel=info





## when production

DEBUG = False
ALLOWED_HOSTS = ["yourdomain.com", "www.yourdomain.com", "server_ip"]
CSRF_COOKIE_SECURE = True
SESSION_COOKIE_SECURE = True
SECURE_SSL_REDIRECT = True
SECURE_HSTS_SECONDS = 31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS = True
SECURE_HSTS_PRELOAD = True



CELERY_BROKER_URL = "redis://:strongpassword@127.0.0.1:6379/0"

CHANNEL_LAYERS = {
    "default": {
        "BACKEND": "channels_redis.core.RedisChannelLayer",
        "CONFIG": {
            "hosts": ["redis://:password@127.0.0.1:6379/0"],
        },
    },
}
